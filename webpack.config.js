
module.exports = {
    // Các cấu hình khác của Webpack
    resolve: {
        fallback: {
            "os": false
        }
    },
    // Các phần khác của cấu hình Webpack như module, plugins, v.v.
};
